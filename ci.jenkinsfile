#!groovy


pipeline{
  agent{ node{ label "master"}  }
  stages{
    stage("build"){
      steps{
        script{
          mvnhome = tool "M3"
          sh "${mvnhome}/bin/mvn -v"
        }
      }
    }
  }
}
