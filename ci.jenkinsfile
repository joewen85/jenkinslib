#!groovy

String mvn_command = ${env.mvn_command}
pipeline{
  agent{ node{ label "master"}  }
  stages{
    stage("build"){
      steps{
        script{
          mvnhome = tool "M3"
          sh "${mvnhome}/bin/mvn ${mvn_command}"
        }
      }
    }
  }
}
