#!groovy


pipeline{
  agent{ node{ label "master"}  }
  stages{
    stage("build"){
      steps{
        scritp{
          mvnHome = tool "M3"
          sh "${mvnHome}/bin/mvn -v"
        }
      }
    }
  }
}
