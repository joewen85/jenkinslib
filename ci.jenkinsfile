#!groovy


pipeline{
  agent{ node{ label "master"}  }
  stages{
    stage("build"){
      steps{
        scritp{
          def mvnhome = tool "M3"
          sh "${mvnhome}/bin/mvn -v"
        }
      }
    }
  }
}
